package trilateral3;
import trilateral3.matrix.MatrixDozen;
class Trilateral{
    // should I make this private to force use via abstracts on pen
    public static
    var transformMatrix: Null<MatrixDozen> = null;
}
